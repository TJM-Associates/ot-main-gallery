config: 
  target: 'http://localhost:3002'
  phases:
    - duration: 60
      arrivalRate: 1000
  payload:
    path: "./stress.csv"
    fields: 
      - "restaurant_name"

scenarios:
  - flow:
    - get:
        url: "/api/restaurants/{{ restaurant_name }}/images"  